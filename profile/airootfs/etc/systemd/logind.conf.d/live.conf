[Login]
# Closing the lid must NOT suspend a live medium: it would wake into a lock
# screen whose password nobody chose. Same reason as the hypridle listener that
# nidara-live-setup strips.
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleSuspendKey=ignore
HandleHibernateKey=ignore
