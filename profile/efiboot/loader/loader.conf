timeout 5
default 01-nidara-linux.conf
# Every console-mode change is one more black flicker between the firmware
# logo and the desktop. `keep` stays with the mode the firmware already set.
console-mode keep
